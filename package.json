{
  "name": "triangle-travel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 5173 --open",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "seed": "go run ./cmd/seed",
    "server": "go run .",
    "build:full": "npm run build && go build -o triangle_travel ."
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.9",
    "@sveltejs/kit": "^2.42.2",
    "@sveltejs/vite-plugin-svelte": "^6.2.0",
    "svelte": "^5.39.4",
    "svelte-check": "^4.3.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.6"
  }
}
